import styled from "styled-components";
import { COLOR_HIGHLIGHT } from "style/styleVariables";

const TabVal = styled.dd`
  margin: 0;
  display: inline-block;
  color: ${COLOR_HIGHLIGHT};
`;

export default TabVal;
