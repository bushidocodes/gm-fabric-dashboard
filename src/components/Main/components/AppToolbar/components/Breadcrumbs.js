import styled from "styled-components";

const Breadcrumbs = styled.ol`
  flex: 1 1 auto;
  display: flex;
  margin: 0;
  padding: 0;
  flex-direction: row;
  height: inherit;
  align-items: stretch;
`;

export default Breadcrumbs;
