import styled from "styled-components";

const Breadcrumbs = styled.ol`
  align-items: stretch;
  display: flex;
  flex: 1 1 auto;
  flex-direction: row;
  height: inherit;
  margin: 0;
  padding: 0;
`;

export default Breadcrumbs;
